{
  "name": "clijack",
  "version": "0.1.0",
  "description": "A library for enhancing command-line tools with custom shortcuts and output handling",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc && mkdir -p dist/examples && cp examples/cli.sh dist/examples/",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.28",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.2"
  }
}
